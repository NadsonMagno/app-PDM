import { StyleSheet } from "react-native";
export const styles = StyleSheet.create({
    input: {
        height: 52,
        borderColor: "gray",
        borderWidth: 1,
        borderRadius: 10,
        width: "100%",
        padding: 11,
        fontSize: 16,
    },
});